database:
  junior:
    Database Design Basics:
    - What is database normalization? Explain 1NF, 2NF, and 3NF with examples.
    - What are primary keys, foreign keys, and constraints in SQL databases?
    - How do database indexes work? When should you create an index?
    - What is an Entity-Relationship (ER) diagram? Explain one-to-one, one-to-many, and many-to-many relationships.
    - What SQL data types exist and how do you choose the right type for a column?
    - What is a database (DB) and a database management system (DBMS)? Which databases are called relational? How are relationships
      between tables established and what types of relationships exist?
    MySQL Basics:
    - What storage engines does MySQL support? Compare InnoDB and MyISAM.
    - What MySQL-specific administrative commands exist (SHOW, DESCRIBE, EXPLAIN)?
    - How does AUTO_INCREMENT work in MySQL? How do you get the last inserted ID?
    - What are character sets and collations in MySQL? How do they affect string comparison?
    - How do you create and manage databases and users in MySQL?
    Redis Basics:
    - What is Redis and how does it differ from a relational database?
    - What Redis data structures would you use for a user session vs a user profile hash?
    - What is TTL in Redis? How do EXPIRE and SET with EX option work?
    - Explain the cache-aside pattern with Redis. What happens on cache miss?
    - What are Redis Lists and Sets used for? Give a practical example of each.
    - What are Redis key naming best practices? Why avoid KEYS * in production?
    - How should you serialize Python objects stored in Redis?
    - How do you connect to Redis from Python? What is the difference between redis-py sync and redis.asyncio?
    SQL Basics:
    - Explain the basic structure of a SELECT query. How does WHERE clause filter data?
    - How do INSERT, UPDATE, and DELETE statements work? What are the risks of each?
    - What are the different types of JOINs in SQL? Explain INNER, LEFT, RIGHT, and FULL OUTER JOIN.
    - How does GROUP BY work with aggregate functions? What is the difference between WHERE and HAVING?
    - How do ORDER BY, LIMIT, and DISTINCT work in SQL queries?
    - What are JOINs in SQL? What types of JOINs exist?
    - What is the difference between DDL, DML, DCL and TCL? Which commands belong to each group? How does `DELETE` differ
      from `TRUNCATE`?
    - What are constraints in SQL? List the main types of constraints.
    - What are SQL injections and what are the ways to protect against them?
    SQLite Basics:
    - When is SQLite a good choice vs PostgreSQL or MySQL?
    - What is WAL (Write-Ahead Logging) mode in SQLite? What benefits does it provide?
    - What are SQLite's concurrency limitations?
    - How does SQLite type affinity work? How is it different from PostgreSQL strict typing?
    - How do you use SQLite from Python? Compare sqlite3 stdlib and aiosqlite.
    - What constraints and indexes does SQLite support?
    - How do you backup a SQLite database safely while the app is running?
    - What practical size and deployment limits should you know for SQLite?
  middle:
    Database Design:
    - When would you denormalize a database schema? What techniques exist?
    - What is table partitioning? How does it differ from sharding?
    - How do you design an effective indexing strategy? What are composite indexes and column order?
    - How do you model polymorphic associations and inheritance in relational databases?
    - How do you handle schema migrations in production with zero downtime?
    - What is Object-Relational Mapping (ORM)? What is `Metadata` in SQLAlchemy?
    - What are partitioning, replication and sharding? What are their main goals?
    Locking and Concurrency:
    - Explain the four standard transaction isolation levels. What anomaly does each prevent?
    - What does SELECT FOR UPDATE do? When would you use it?
    - What is a database deadlock? How do databases detect and resolve them?
    - Compare optimistic and pessimistic locking. How is optimistic locking implemented with a version column?
    - How does MVCC in PostgreSQL affect readers and writers?
    - What is the difference between row-level and table-level locks?
    - What transaction boundaries should application code respect?
    - How do Django and SQLAlchemy manage transactions by default?
    Schema Migrations:
    - What is Alembic and how does it relate to SQLAlchemy?
    - How do Django migrations work? What is the difference between makemigrations and migrate?
    - How do you roll back a failed migration safely?
    - What is a zero-downtime migration strategy for adding a NOT NULL column?
    - How do you detect schema drift between environments in CI?
    - How do you handle parallel migration branches in Alembic?
    - How do you add an index to a large PostgreSQL table with minimal locking?
    - 'When should migrations run in deployment: before or after new code?'
    NoSQL Databases:
    - What types of NoSQL databases exist? Compare document, key-value, wide-column, and graph databases.
    - How does MongoDB's aggregation pipeline work? How do you index documents?
    - What data structures does Redis support beyond simple key-value? How do you use Redis for caching?
    - How does Apache Cassandra model data? Explain partition key, clustering columns, and CQL.
    - How do NoSQL databases apply the CAP theorem and BASE model?
    PostgreSQL:
    - How does PostgreSQL implement MVCC (Multi-Version Concurrency Control)? What is VACUUM?
    - What advanced data types does PostgreSQL support? How do you use JSONB for semi-structured data?
    - What PostgreSQL extensions exist and how do they extend functionality?
    - How do you configure PostgreSQL for performance and security?
    - How does PostgreSQL replication work? Explain streaming replication and logical replication.
    - What does the `VACUUM` command do in PostgreSQL?
    Redis Advanced:
    - How does Redis Pub/Sub work? What are its delivery guarantees?
    - What is a Redis pipeline? Why use it?
    - How do Redis transactions (MULTI/EXEC) work? What is WATCH?
    - Compare RDB snapshots and AOF persistence in Redis.
    - What happens when Redis reaches maxmemory? Explain eviction policies.
    - How does Redis Cluster shard data across nodes?
    - What are Redis Streams? How do consumer groups work?
    - How is Redis used as a Celery broker and result backend?
    Advanced SQL:
    - How do window functions work in SQL? Explain ROW_NUMBER, RANK, and SUM with OVER.
    - What are Common Table Expressions (CTEs) and recursive queries in SQL?
    - What is the difference between correlated and non-correlated subqueries? When is EXISTS preferred over IN?
    - What are transaction isolation levels (READ UNCOMMITTED, READ COMMITTED, REPEATABLE READ, SERIALIZABLE)?
    - How do you read EXPLAIN ANALYZE output?
    - What are views and materialized views in SQL? When would you use each?
    - What are indexes and how do they affect query performance? How do clustered and non-clustered indexes differ? What types
      of indexes exist and which index is used by default in PostgreSQL?
    - What is column selectivity and how does it affect the query planner? What are `EXPLAIN` and `EXPLAIN ANALYZE`?
    - What is a subquery and what types of subqueries exist? What is a Common Table Expression (CTE)?
    - In what order is an SQL query logically executed? What are aggregate functions and how do they differ from window functions?
    - What is a cursor in databases and for what purposes is it used?
    - How are slow queries typically optimized? What is the essence of the N+1 problem and how is it solved with SQLAlchemy?
    - What is a transaction and a nested transaction? What does the acronym ACID stand for? What transaction isolation levels
      exist?
  senior:
    ClickHouse Analytics:
    - When should you choose ClickHouse over PostgreSQL?
    - What is the MergeTree engine family in ClickHouse?
    - How does partitioning work in ClickHouse? What are best practices?
    - Why are aggregate queries fast in ClickHouse? What is vectorized execution?
    - What are best practices for ingesting high-volume events into ClickHouse?
    - How does ClickHouse replication and sharding work at a high level?
    - How would you design a pipeline from PostgreSQL OLTP to ClickHouse analytics?
    - How does ClickHouse handle duplicate rows? What is FINAL?
    Distributed Databases:
    - How does database sharding work? How does consistent hashing distribute data?
    - How do distributed SQL databases (CockroachDB, Google Spanner, YugabyteDB) work?
    - How does leader election and automatic failover work in distributed databases?
    - How do you design a multi-region database topology? What are the trade-offs?
    - How do you design backup and disaster recovery strategies for databases?
    Performance Tuning:
    - How do you identify and fix slow database queries? How do you deal with the N+1 query problem?
    - How do you maintain indexes and manage index bloat in high-write databases?
    - How do connection poolers work? Compare PgBouncer modes and connection management strategies.
    - How do you tune autovacuum in PostgreSQL for high-volume tables?
    - How do you benchmark database performance? What metrics matter?
    - How do you distribute read load across replicas? What are the trade-offs?
    Search Databases:
    - How does Elasticsearch index and search data? What is an inverted index?
    - How do Elasticsearch shards and replicas work? How do you plan capacity?
    - What are Elasticsearch mappings and analyzers? How do you customize text analysis?
    - How do you tune search relevance and ranking in Elasticsearch?
    - What is the ELK stack (Elasticsearch, Logstash, Kibana)? How do you use it for log aggregation?
    - Compare Elasticsearch and OpenSearch. When would you choose one over the other?
python:
  junior:
    Asynchronous Programming:
    - What is the GIL (Global Interpreter Lock) and why is it needed? Does it affect the execution of asynchronous code?
    - What are concurrency and parallelism? What problems are solved with multithreading, multiprocessing, and asynchrony?
    - What is a coroutine and how does it differ from an ordinary function?
    - What is an event loop and how does it manage the execution of asynchronous tasks?
    - How are generators related to asynchronous code?
    - How to run sequential, background, and group execution of asynchronous tasks? What are their differences?
    - What is an asynchronous iterator and an asynchronous context manager?
    - How to limit the execution time of a coroutine (timeout)?
    - What is the difference between asyncio.create_task() and asyncio.gather()? When would you use each?
    - Why should you avoid time.sleep() and blocking I/O inside async def functions?
    - What is a common junior mistake when calling async functions? Explain why 'result = my_coro()' is wrong.
    Basics:
    - What is the difference between mutable and immutable types in Python? Give examples of each.
    - Explain the difference between '==' and 'is' in Python.
    - What values are considered falsy in Python? How does 'None' differ from other falsy values?
    - What is type hinting in Python 3.10+? Why is it useful? Describe the typing features added in Python 3.10.
    - Explain the difference between shallow copy and deep copy in Python.
    - What are dunder methods (magic methods) in Python? Give three examples and explain when they are called.
    - What is the difference between an interpreter and a compiler? How is Python code executed and what processes occur when
      it runs?
    - How does Python manage memory allocation and deallocation? How does the garbage collector work?
    - What are magic (dunder) methods? Which of them are called when an instance of a class is created? Which magic methods
      does a context manager implement (the with protocol) and why is it used?
    Control Flow:
    - Explain how Python evaluates 'if', 'elif', and 'else' blocks. What happens if multiple conditions are True?
    - What are the different ways to create loops in Python? Compare 'for' and 'while' loops.
    - How does 'range()' work in Python? What is 'enumerate()' and why is it useful?
    - What are comprehensions in Python? Explain list, dict, and set comprehensions.
    - How does the 'with' statement work in Python? What protocol does it use?
    - What is structural pattern matching (match-case) in Python 3.10? Give a basic usage example.
    - How does 'zip()' work in Python? What happens when iterables have different lengths?
    - What are the differences between an iterator (iterator), an iterable (iterable) and a generator (generator)? What methods
      does a generator have (besides `__next__` and `send`)?
    Data Structures:
    - What is the difference between a list and a tuple in Python?
    - How does a Python dictionary work internally?
    - What is the difference between a set and a frozenset in Python? When would you use each?
    - How does list slicing work in Python? What are common list methods for manipulation?
    - What are common dictionary methods? How does collections.defaultdict simplify dictionary usage?
    - What useful data structures does the 'collections' module provide? Describe three of them.
    - What is the difference between list, array.array, and numpy.array? When would you choose each?
    - How would you implement a stack (LIFO) and a queue (FIFO) in Python?
    - What data types exist in Python and into which groups can they be divided? How does a tuple differ from a list? What
      is the time complexity of appending an element to a list (e.g., `append` method)?
    - How are dictionaries (`dict`) implemented and what objects can serve as keys? What is a hash table and is it possible
      for different objects to have the same hash?
    Django REST Framework:
    - What is a Serializer in Django REST Framework? How does it differ from a Django Form?
    - What is ModelSerializer? How does it relate to Django models?
    - What is APIView in DRF? How do you handle GET and POST in one class?
    - What is a ViewSet and DefaultRouter in DRF? What endpoints do they generate?
    - What HTTP status codes should a REST API return for successful GET, POST, and DELETE?
    - Why is pagination important in REST APIs? How does DRF PageNumberPagination work?
    - What are generic views (ListAPIView, CreateAPIView) in DRF? When are they useful?
    - What are renderers and parsers in DRF? How does JSON request parsing work?
    Django:
    - What is Django's MVT pattern? How do Model, View, and Template relate?
    - How do you define a Django model? What is a migration and why is it needed?
    - What is a QuerySet in Django ORM? Explain filter(), get(), and all().
    - What is Django Admin? How do you register a model in the admin site?
    - How does URL routing work in Django? What is the difference between FBV and CBV?
    - What is settings.py in Django? Why should DEBUG=False and SECRET_KEY be protected in production?
    - What is a Django app vs a Django project? How do you add a new app?
    - What is Django middleware? Name two built-in middleware classes and their purpose.
    Exceptions:
    - Explain the try/except/else/finally blocks in Python. What is the purpose of each?
    - What is the Python exception hierarchy? What is the difference between BaseException, Exception, and KeyboardInterrupt?
    - How do you raise and create custom exceptions in Python?
    - How do exceptions interact with context managers? What happens if __exit__ returns True?
    - What does the 'assert' statement do in Python? When should you use it vs raising exceptions?
    FastAPI:
    - What is the difference between WSGI and ASGI? Why does FastAPI use ASGI?
    - How do you define HTTP routes in FastAPI? Explain path parameters vs query parameters.
    - How does Pydantic validate request bodies in FastAPI? What happens on validation failure?
    - What is FastAPI's Depends() used for? Give a simple example of dependency injection.
    - How does FastAPI generate API documentation? What URLs are available by default?
    - How do you test a FastAPI endpoint without running a real server? What is TestClient?
    - How do you return custom HTTP status codes and error responses in FastAPI?
    - Should FastAPI path operation functions be async def or def? When does it matter?
    Functions:
    - Explain the difference between positional arguments, keyword arguments, *args, and **kwargs in Python?
    - What are lambda functions in Python? When should you use them vs regular named functions?
    - Explain Python's LEGB scoping rule. How does it affect variable lookup?
    - What is the problem with mutable default arguments in Python? Why can it lead to bugs?
    - How does the 'return' statement work in Python? What does a function return if there's no return?
    - What are function annotations (type hints) in Python? How do you document a function properly?
    - What does it mean that functions are first-class citizens in Python? Give examples.
    - What is a function in Python and what is a pure function? What is an anonymous function (lambda)?
    - How does variable scope work in Python? What is a closure and how is it formed?
    - Define a decorator. What can act as a decorator and to which elements (functions, methods, classes) can it be applied?
    Object-Oriented Programming:
    - What is the difference between a class and an instance in Python? What is 'self'?
    - How does inheritance work in Python? What is Method Resolution Order (MRO)?
    - How does Python handle visibility and encapsulation? Explain single underscore, double underscore, and name mangling.
    - How do you create getters and setters in Python? Explain the @property decorator.
    - What are dataclasses in Python? How do they differ from regular classes and __slots__?
    - Explain the difference between inheritance and composition. When should you prefer composition?
    - How do you create abstract classes in Python? What is the ABC module?
    - What is object-oriented programming and what are its main principles? What is MRO (Method Resolution Order) and what
      is `super()` used for?
    - How do class methods (`@classmethod`) differ from static methods (`@staticmethod`)? What methods are called abstract?
      What is `__slots__` and why is it needed?
    pytest:
    - How does pytest differ from the unittest module in the standard library?
    - How does pytest discover tests? What naming conventions does it use by default?
    - How does pytest improve plain assert statements? What happens on failure?
    - What is a pytest fixture? How do you declare and use one?
    - What does @pytest.mark.parametrize do? Give an example testing multiple inputs.
    - What is conftest.py in pytest? How are fixtures shared across test modules?
    - How do you test that code raises an expected exception in pytest?
    - How do you run a subset of tests with pytest using -k and -m?
    Strings:
    - What are the different ways to format strings in Python? Compare f-strings, .format(), and %-formatting.
    - Are strings mutable or immutable in Python? How does this affect common string operations?
    - How does string slicing work in Python? Explain negative indexing and step values.
    - Why is ''.join(list) preferred over string concatenation in a loop?
    - How does Python handle Unicode and string encoding? What is the difference between str and bytes?
    - What are raw strings in Python? When would you use them?
    Kafka Client Basics:
    - What are the main Python libraries for Kafka? Compare kafka-python, confluent-kafka-python, and aiokafka (pros, cons,
      performance, use cases).
    - What is the structure of a Kafka producer in confluent-kafka-python? Explain JSON serialization,
      delivery callbacks, and the purpose of poll() and flush().
    RabbitMQ Client Basics:
    - What are the main Python client libraries for RabbitMQ? Compare pika, aio-pika, and kombu. Which one is suitable for
      sync, async, and production use?
    - What is the structure of a RabbitMQ producer in pika? Explain publisher confirms, mandatory flag,
      message persistence, and exchange declaration.
  middle:
    Architecture:
    - Explain the SOLID principles. How do they apply to Python development?
    - What is Clean Architecture? How does it relate to Domain-Driven Design (DDD)?
    - What is dependency injection? How do you implement it in Python without a framework?
    - What is CQRS (Command Query Responsibility Segregation) and when should you use it?
    - What are the Repository and Unit of Work patterns? How do they work together?
    - What is hexagonal architecture (ports and adapters)? How does it differ from layered architecture?
    - What is layered (n-tier) architecture? What are its strengths and weaknesses compared to hexagonal or clean architecture?
    Asyncio Advanced:
    - What is asyncio.TaskGroup (Python 3.11+)? How does it improve structured concurrency?
    - How does asyncio.Queue work? Give a producer-consumer example.
    - What synchronization primitives does asyncio provide? When use Lock vs Semaphore?
    - What is loop.run_in_executor()? When must you use it?
    - How does task cancellation work in asyncio? What is asyncio.shield()?
    - Compare asyncio.wait_for, asyncio.timeout, and manual cancellation for timeouts.
    - What are common asyncio antipatterns in production code?
    - How do async HTTP clients and DB drivers integrate with asyncio?
    Django REST Framework:
    - How do permission classes work in DRF? Compare IsAuthenticated and DjangoModelPermissions.
    - What is throttling in DRF? How do you rate-limit API clients?
    - How do nested serializers represent related objects in DRF?
    - How do you filter querysets in DRF? What does django-filter integration provide?
    - What API versioning strategies does DRF support?
    - How do you test DRF APIs with APIClient?
    - What authentication classes does DRF provide? Compare SessionAuthentication and TokenAuthentication.
    - How do you add custom actions to a ViewSet with @action?
    Django:
    - What is the N+1 query problem in Django? How do select_related and prefetch_related help?
    - How do custom managers and QuerySets work in Django?
    - What are Django signals? When should you use them and when avoid them?
    - How does Django's caching framework work? Compare per-view vs low-level cache API.
    - How do Django permissions and groups work? How do you check permissions in a view?
    - What is CSRF protection in Django? How does the CSRF token work for forms and AJAX?
    - How do database transactions work in Django? What does atomic() do?
    - Explain the Django middleware request/response chain. How do you write custom middleware?
    FastAPI:
    - How do FastAPI dependencies with yield work? Why are they useful for database sessions?
    - What is ASGI middleware in FastAPI? How do you configure CORS?
    - How do you register global exception handlers in FastAPI?
    - What are BackgroundTasks in FastAPI? When should you use them vs Celery?
    - What is the lifespan context manager in FastAPI? What runs at startup and shutdown?
    - How do you integrate FastAPI with SQLAlchemy 2.0 and asyncpg?
    - How does OAuth2PasswordBearer work in FastAPI? Outline the login and protected route flow.
    - How do WebSocket endpoints work in FastAPI?
    Frameworks:
    - How does FastAPI work under the hood? What makes it high-performance?
    - What is Django's architecture? Explain the ORM, views, templates, and middleware layers.
    - How does Flask differ from FastAPI and Django? When would you choose Flask?
    - Compare Django ORM and SQLAlchemy. What are the key differences in philosophy and usage?
    - How does pytest compare to unittest? What makes pytest the preferred testing framework in modern Python?
    - How does FastAPI's dependency injection system work? What can you use it for?
    - What is the difference between WSGI and ASGI? Why does ASGI matter for modern Python web apps?
    Metaprogramming:
    - What are metaclasses in Python? How do you create one and when would you use them?
    - What are descriptors in Python? How do __get__, __set__, and __delete__ work?
    - What is the difference between a function decorator and a class-based decorator? When would you use each?
    - How does __init_subclass__ work? How is it different from a metaclass?
    - What is AST (Abstract Syntax Tree) in Python? How can you use it for code analysis or transformation?
    - What tools does Python's inspect module provide for runtime introspection?
    - What is the difference between __getattr__ and __getattribute__? How do they affect attribute lookup?
    - What are magic (dunder) methods? Which of them are called when an instance of a class is created? Which magic methods
      does a context manager implement (the `with` protocol) and why is it used?
    - What are descriptors? How when calling a method through a class instance the instance automatically gets passed as the
      first argument (`self`)? What is the built-in `property` used for?
    Packaging and Distribution:
    - What is pyproject.toml? How does it replace setup.py and setup.cfg?
    - Compare pip, pipenv, poetry, and uv for dependency management. What are the trade-offs?
    - What is the difference between a source distribution (sdist) and a wheel? How are they built?
    - How do you publish a Python package to PyPI? What are the steps?
    - How do you create CLI entry points for a Python package? What are console_scripts?
    - What are namespace packages in Python? How do implicit namespace packages work?
    pytest:
    - What fixture scopes exist in pytest? When use session vs function scope?
    - What does autouse=True on a fixture do?
    - What is the monkeypatch fixture in pytest? Give examples of what it can patch.
    - How do capsys and caplog fixtures help test output and logging?
    - How do you test async code with pytest-asyncio?
    - How do unittest.mock and pytest-mock differ? What is the mocker fixture?
    - How do pytest plugins extend functionality? How is coverage typically integrated?
    - Why use factory_boy with pytest instead of creating model instances manually?
    Type Hints:
    - How do generics work in Python? Explain TypeVar, Generic, and bounded type variables.
    - What are Protocols in Python? How do they enable structural subtyping?
    - How does @typing.overload work? How does it differ from overloading in other languages?
    - How does mypy work? What are the benefits of static type checking in a Python project?
    - What are Literal, TypedDict, and Final types in Python? How do they improve type safety?
    - What tools does typing provide for type narrowing and debugging type hints?
    Kafka Client Development:
    - Compare auto commit vs manual offset commit in Kafka consumers. Explain synchronous vs asynchronous
      commit and when to use each approach.
    - How does an async Kafka consumer work with aiokafka inside a FastAPI application? Explain startup/shutdown
      lifecycle, concurrency control, and backpressure handling.
    - How to handle message processing failures and retries in Python Kafka consumer? Discuss dead-letter queue pattern, poison
      pills, and exponential backoff. How to implement custom retry with commit control?
    - 'What metrics and tools are essential for monitoring Python Kafka applications? How to measure consumer lag, producer
      throughput, errors. How to debug common issues: ''message too large'', ''broker not available'', ''rebalance timeout''.'
    RabbitMQ Client Development:
    - What is the difference between basic_ack, basic_nack, and basic_reject in RabbitMQ? Explain
      how QoS prefetch count, manual acknowledgements, and graceful shutdown work in a pika consumer.
    - How does an async RabbitMQ consumer work with aio-pika? Explain the role of connect_robust,
      manual ack, backpressure handling via Semaphore, and FastAPI startup/shutdown integration.
    - How to handle RabbitMQ connection failures and reconnections in Python? Discuss heartbeat settings, connection recovery
      strategies (exponential backoff, retry limits), and channel recovery.
    - How to handle message serialization in RabbitMQ Python clients? Compare JSON, MessagePack, Avro, and Protobuf.
      How does the content_type header help the consumer choose the correct deserializer?
  senior:
    System Design:
    - How do consensus algorithms like Raft and Paxos work? When would you need them in a Python system?
    - How do you design and orchestrate data pipelines in a Python ecosystem?
    - What is an Architectural Decision Record (ADR)? How do you use them in a Python project?
    - What is chaos engineering? How do you apply it to Python microservices?
    Asyncio Production:
    - How do you debug a hung or slow asyncio event loop in production?
    - How do you integrate synchronous legacy code with an async FastAPI/Django ASGI app?
    - What is backpressure in async systems? How do you implement it with asyncio.Queue?
    - What metrics and logs should you monitor for async Python services?
    - How do you implement reliable shutdown for long-running asyncio services?
    - How do you test race conditions and timing-dependent asyncio code?
    Django Production:
    - How do you optimize Django ORM queries under high load?
    - How does connection pooling work with Django and PostgreSQL?
    - How do you route reads to replicas in a Django application?
    - How does Celery integrate with Django? Describe broker, workers, and result backend.
    - When should you use Celery vs Django BackgroundTasks or synchronous code?
    - How does Django multi-database routing work?
    - What production security hardening steps are essential for Django?
    - Why define a custom User model at project start? What are the migration implications?
    FastAPI Production:
    - How do you deploy FastAPI in production with Uvicorn and Gunicorn workers?
    - What is graceful shutdown for an ASGI app? How do lifespan and signal handling help?
    - How do you implement rate limiting in FastAPI at scale?
    - How do you add observability (logs, metrics, traces) to a FastAPI service?
    - What security checklist applies to FastAPI APIs in production?
    - How would you design multi-tenant authentication and data isolation in FastAPI?
    - What patterns improve FastAPI throughput under high load?
    Performance Optimization:
    - How do you profile a Python application? Compare cProfile, py-spy, and flame graphs.
    - How do you detect and debug memory leaks in Python? What tools are available?
    - How does the GIL (Global Interpreter Lock) affect Python performance? When is multiprocessing preferred over threading?
    - How do you optimize performance-critical Python code with C extensions or alternative runtimes?
    - How do you optimize async I/O performance in Python? What patterns improve throughput?
    - How does functools.lru_cache and functools.cache work? When should you use caching to optimize Python code?
    - How do lazy evaluation patterns (generators, iterators, itertools) improve performance in Python?
    Security:
    - How do you implement authentication in a Python web application? Compare session-based auth, JWT, and OAuth2.
    - How do you implement authorization (access control) in Python? Compare RBAC and ABAC.
    - What are the most common web vulnerabilities and how do you prevent them in Python?
    - How do you handle password storage and encryption in Python?
    - How do you audit Python dependencies for security vulnerabilities? What tools exist?
    - What are the key security considerations for REST APIs and GraphQL in Python?
    - What are SSRF, insecure deserialization, and XXE vulnerabilities? How do they apply to Python?
    Kafka Client Production:
    - How does exactly-once semantics work in Kafka with confluent-kafka-python? Explain the difference between
      idempotent producer and transactional producer/consumer. What configuration is required?
    - How to optimize Python Kafka producer and consumer for maximum throughput and low latency? Discuss batching (batch.size,
      linger.ms), compression (gzip, snappy, lz4, zstd), fetch parameters, and multi-threading vs multi-processing.
    - How does Avro serialization integrate with Kafka in Python? Explain the role of Confluent Schema Registry,
      AvroSerializer, AvroDeserializer, and schema evolution.
    - What patterns exist for parallel Kafka message processing in Python? Compare single consumer + worker
      processes vs process-per-partition. Discuss rebalancing, commit coordination, and backpressure.
    RabbitMQ Client Production:
    - How does the RPC pattern work over RabbitMQ? Explain the roles of correlation_id, reply_to queue,
      timeout handling, and concurrent requests.
    - How does a retry pattern with dead-letter exchange (DLX) work in RabbitMQ? Explain the configuration of
      main queue, retry queue with TTL, and the message flow between them.
system-design:
  middle:
    System Design:
    - Explain the difference between horizontal and vertical scaling. When would you choose each?
    - What is the CAP theorem? How does it affect distributed system design?
    - What caching strategies do you know? Compare cache-aside, write-through, and write-behind.
    - How do you decompose a monolith into microservices? What are the key considerations?
    - Compare RabbitMQ and Apache Kafka. When would you choose each?
    - What load balancing algorithms exist and when would you use each?
    - How do you implement rate limiting in a distributed system? Compare token bucket, leaky bucket, and sliding window.
    - For which tasks and for which types of requests (e.g., read, frequent repeated computations) is caching applied?
    - What typical problems do message queues (e.g., RabbitMQ, Kafka) help solve?
    - What is Domain-Driven Design (DDD)? What is the usefulness of the Repository and Unit of Work patterns?
  senior:
    Advanced Architecture:
    - How do you design an event-driven architecture? What patterns and technologies are involved?
    - What is event sourcing? How do you rebuild read models (projections) and handle versioning?
    - How do you approach real-time stream processing? Compare Apache Kafka Streams, Flink, and ksqlDB.
    - How do you migrate from a monolith to microservices? Explain the strangler fig pattern and incremental migration.
    Distributed Systems:
    - How do you manage distributed transactions in a microservices architecture? Compare 2PC and Saga.
    - What is observability in distributed systems? Explain the three pillars (logs, metrics, traces).
    - What resilience patterns are used in distributed systems? Explain circuit breaker, bulkhead, and retry with backoff.
    - What is a service mesh? How does it work (sidecar proxy, control plane)?
    - How do you handle idempotency and deduplication in distributed systems?
    - What is eventual consistency? How do you handle conflict resolution in distributed databases?
kafka:
  junior:
    Kafka Fundamentals:
    - What is Apache Kafka? What problems does it solve? Compare with traditional message brokers (RabbitMQ, ActiveMQ).
    - Explain topic, partition, offset, broker, replica, leader, ISR (In-Sync Replicas). How does partitioning affect parallelism?
  middle:
    Kafka Messaging:
    - What delivery semantics does Kafka provide (at-most-once, at-least-once, exactly-once)? How to achieve exactly-once
      semantics (EOS) in Kafka?
    - What is consumer group rebalancing? Describe the rebalance protocol (JoinGroup, SyncGroup). What are cooperative vs
      eager rebalancing? How to avoid 'rebalance storm'?
    - What is the acks parameter in Kafka producer? Explain acks=0, acks=1, acks=all. How does it affect durability and latency?
      What is min.insync.replicas?
    Kafka Storage:
    - How does Kafka store messages on disk? What are log segments, rolling, retention policies (time, size), and log compaction?
      What is the difference between deletion and compaction?
  senior:
    Kafka Architecture:
    - What was the role of ZooKeeper in Kafka? What problems did it cause? How does KRaft (Kafka Raft) replace ZooKeeper?
      Explain the new controller quorum and metadata topic.
    - What is Kafka Streams and ksqlDB? How do they differ from ordinary consumers? Explain KStream, KTable, GlobalKTable,
      and windowed joins. When to use ksqlDB vs Kafka Streams?
rabbitmq:
  junior:
    RabbitMQ Fundamentals:
    - What is RabbitMQ? What messaging protocols does it support (AMQP 0-9-1, AMQP 1.0, MQTT, STOMP)? How does it differ from
      Kafka?
    - 'Explain RabbitMQ exchange types: direct, topic, fanout, headers. How do bindings and routing keys work? Give examples
      of when to use each type.'
    - What queue properties exist in RabbitMQ? Explain durable, exclusive, auto-delete, TTL (per-queue and per-message), max
      length, and lazy queues.
  middle:
    RabbitMQ Messaging:
    - What delivery guarantees does RabbitMQ provide? Explain publisher confirms, mandatory/immediate flags, consumer acknowledgements
      (auto, manual), prefetch count, and message persistence.
    - What is Dead Letter Exchange (DLX) in RabbitMQ? How to configure it for queues? When do messages become dead-lettered?
      Show a typical dead-letter queue pattern for failed messages.
  senior:
    RabbitMQ Architecture:
    - 'How does RabbitMQ clustering work? Explain different high availability strategies: classic mirrored queues (HA policy)
      vs quorum queues (based on Raft). What is the difference in data safety, performance, and failover behavior?'
    - 'What are RabbitMQ Streams? How do they differ from classic queues? Explain stream features: non-destructive consumption,
      time-based retention, offset tracking, super-streams, and use cases.'
    - 'Name essential RabbitMQ plugins: management, shovel, federation, delayed message exchange. How to use shovel for moving
      messages between clusters? What is federation for? Explain the use of rabbitmqadmin.'
    RabbitMQ Operations:
    - How to tune RabbitMQ for high throughput? Explain flow control (credit-based), memory alarms, disk free limit, prefetch
      and batch get, TCP settings, and lazy queues vs in-memory.
docker:
  junior:
    Docker Fundamentals:
    - What is Docker? How does a container differ from a virtual machine (VM)? Explain the concept of images, containers,
      and registries (Docker Hub).
    - 'Explain key instructions in a Dockerfile: FROM, RUN, COPY, ADD, CMD, ENTRYPOINT, EXPOSE, ENV, WORKDIR. What is the
      difference between CMD and ENTRYPOINT? Show a multi-stage build example.'
    - How does Docker handle persistent data? Compare volumes, bind mounts, and tmpfs mounts. Explain volume lifecycle (creation,
      backup, removal). When to use named volumes vs bind mounts?
  middle:
    Docker Operations:
    - 'Explain Docker networking drivers: bridge (default), host, none, overlay, macvlan. How does container-to-container
      communication work on the same host and across hosts? What is the difference between user-defined bridge and default
      bridge?'
    - 'What is Docker Compose? Explain version 3 syntax: services, networks, volumes, depends_on, healthcheck, environment.
      How to scale services and handle environment-specific overrides (docker-compose.override.yml)?'
    - 'How to inspect and debug Docker containers? Explain: docker logs, docker exec, docker stats, docker top, docker events.
      What logging drivers are available (json-file, syslog, journald, fluentd)? How to limit log size?'
  senior:
    Docker Security:
    - 'What are Docker security best practices? Explain: running as non-root user, dropping Linux capabilities, seccomp profiles,
      AppArmor/SELinux, and rootless Docker. How to scan images for vulnerabilities (e.g., Trivy, Docker Scout)?'
kubernetes:
  junior:
    Kubernetes Fundamentals:
    - Describe the Kubernetes architecture. Explain control plane components (API server, etcd, scheduler, controller manager)
      and node components (kubelet, kube-proxy, container runtime). What is a Pod?
    - Explain the Pod lifecycle phases (Pending, Running, Succeeded, Failed, Unknown). What are init containers, postStart/preStop
      hooks, and liveness/readiness/startup probes? How to define them?
    - 'Explain Kubernetes workload resources: Deployment, ReplicaSet, StatefulSet, DaemonSet, Job, CronJob. When to use each?
      How does a RollingUpdate differ from Recreate strategy?'
    - How to manage configuration in Kubernetes? Explain ConfigMap and Secret. How to inject them as environment variables
      or volume mounts? What are the limitations of Secrets (size, etc.)? How to update config without restarting pods?
  middle:
    Kubernetes Networking and Storage:
    - 'Explain Kubernetes Service types: ClusterIP, NodePort, LoadBalancer, ExternalName. What is a headless service? How
      does Ingress differ from LoadBalancer? Describe kube-proxy modes (iptables, IPVS).'
    - How does storage work in Kubernetes? Explain PersistentVolume (PV), PersistentVolumeClaim (PVC), StorageClass, and volume
      modes (Filesystem, Block). What are the reclaim policies (Retain, Delete, Recycle)? How to use CSI drivers?
    Kubernetes Scheduling and Scaling:
    - How does pod scheduling work in Kubernetes? Explain nodeSelector, nodeAffinity (required/preferred), podAffinity/antiaffinity,
      taints and tolerations. What is the role of priorityClassName and preemption?
    - 'Explain autoscaling in Kubernetes: Horizontal Pod Autoscaler (HPA) based on CPU/memory and custom metrics. What is
      Vertical Pod Autoscaler (VPA)? How does Cluster Autoscaler work? What are the prerequisites (metrics-server)?'
  senior:
    Kubernetes Production:
    - 'What is an Ingress Controller? Compare NGINX Ingress, Traefik, and AWS ALB Ingress Controller. How to implement TLS
      termination with cert-manager and Let''s Encrypt? Explain Ingress rules (path types: Exact, Prefix, ImplementationSpecific).'
    - 'Explain Kubernetes security: RBAC (Roles, ClusterRoles, RoleBindings, ClusterRoleBindings), ServiceAccounts, PodSecurityStandards
      (PSP deprecated) and PodSecurity admission. How to restrict container privileges (securityContext: runAsNonRoot, allowPrivilegeEscalation,
      readOnlyRootFilesystem)?'
observability:
  junior:
    Prometheus Fundamentals:
    - Explain the Prometheus architecture. How does the pull model work? What components (Prometheus server, TSDB, exporters,
      Pushgateway, Alertmanager) exist and what are their roles?
    - What metric types does Prometheus support? Explain Counter, Gauge, Histogram, Summary. How to choose between Histogram
      and Summary? Provide example metrics with _total, _bucket suffixes.
    Grafana Basics:
    - What is Grafana? Explain the concept of dashboards, panels, rows, and data sources (Prometheus, Loki, etc.). How to
      create a simple graph panel with a Prometheus query and set basic visualization options (legend, axes, thresholds)?
    - 'How to use Grafana variables (templating)? Explain variable types: query, interval, custom, constant. Show an example
      of a variable that lists all instances from Prometheus, and use it in a panel query. How to use $__rate_interval and
      $__interval?'
    - What types of plugins exist in Grafana (data source, panel, app)? How to install a community plugin (e.g., Pie Chart,
      Clock, or Infinity data source)? What are the risks of using custom plugins?
    Loki Fundamentals:
    - 'What is Loki? How does it differ from Elasticsearch (ELK)? Explain Loki architecture: distributor, ingester, querier,
      compactor, index (boltdb-shipper or TSDB). What is the role of Promtail (or Grafana Agent)?'
    - How to integrate Loki with Grafana? Show how to add Loki data source, use Explore for log queries, and create a dashboard
      with logs panel. How to switch from logs to metrics (and vice versa) using derived fields (extract trace_id to link
      to Tempo).
  middle:
    Prometheus Operations:
    - 'Write PromQL queries to: (a) get current memory usage, (b) calculate per-second rate of HTTP requests over 5 minutes,
      (c) get 95th percentile of request duration, (d) compare current CPU to 1 hour ago. Explain vector matching (on, ignoring,
      group_left/right).'
    - How to monitor infrastructure with Prometheus exporters? Explain node_exporter, blackbox_exporter, and cAdvisor. How
      does Prometheus service discovery work in Kubernetes (kubernetes_sd_config)? Show relabel_configs to filter and rewrite
      labels.
    - 'How to define Prometheus alerting and recording rules? Show an example of alert rule (high CPU usage) and recording
      rule (aggregate request rate). Explain Alertmanager configuration: routes, receivers, inhibition, group_by, group_wait/group_interval/repeat_interval.'
    Grafana Operations:
    - 'Explain Grafana Alerting (unified). How does it differ from legacy dashboard alerts? Describe the components: data
      source queries, expressions (Reduce, Math, Resample), alert rule evaluation, contact points, notification policies,
      silences.'
    - What is Grafana provisioning (as code)? Explain how to provision data sources, dashboards (via JSON), and alerting resources
      using YAML files. How to set up folder structure and dashboard version control with Git?
    Loki Operations:
    - 'Write LogQL queries for: (a) filter all logs with ''error'' in the message, (b) show error count per minute, (c) parse
      JSON log and extract a field, (d) calculate rate of error logs over 5 minutes. Explain line filters (=, !=, =~, !~)
      and label matchers.'
    - How does Loki multitenancy work? How to configure tenant separation via X-Scope-OrgID header? How to set up Grafana
      to send logs to a specific tenant (using custom HTTP headers)? Explain how to query across tenants (as admin).
    - 'How to use Promtail pipeline stages? Explain docker_sd, file_targets, and stages: docker, regex, json, template, labels,
      output, timestamp, replace. Show an example of extracting level and module from a log line and adding them as labels.'
  senior:
    Prometheus at Scale:
    - How to scale Prometheus for multi-cluster or long-term storage? Explain hierarchical federation (with pros/cons), remote_write/remote_read,
      and Thanos/Cortex/VictoriaMetrics architecture. What are the limitations of federation and when to use Thanos instead?
    - What are Prometheus exemplars? How do they link metrics with traces? Show an example of exemplar injection in client
      library (OpenTelemetry) and querying with PromQL (exemplar $__rate_interval).
    Loki Storage:
    - How does Loki store data in object storage (S3, GCS, MinIO)? Explain the boltdb-shipper and TSDB index modes. What are
      the advantages of TSDB index over boltdb-shipper? How to run the compactor and what does it do?
airflow:
  junior:
    Airflow Fundamentals:
    - 'Explain Apache Airflow architecture. What are the core components: Scheduler, Webserver, Executor, Worker, Metadata
      Database? How do they interact? What is a DAG and what is its purpose?'
    - What are Operators, Sensors, and Hooks in Airflow? Name common operators (BashOperator, PythonOperator, SQLExecuteQueryOperator,
      etc.). How do Sensors differ from Operators? What is the purpose of Hooks?
    - How to define task dependencies in Airflow? Explain bitshift operators (>>, <<), set_upstream/set_downstream, and chaining.
      What is the difference between cross-DAG dependencies (ExternalTaskSensor) and trigger DAGs (TriggerDagRunOperator)?
    Airflow Configuration:
    - How to manage configuration and secrets in Airflow? Explain Variables (using Airflow's UI or CLI) and Connections. How
      to avoid hardcoding passwords? What is a secrets backend (e.g., HashiCorp Vault, AWS Secrets Manager)?
    - How to pass data between tasks in Airflow? Explain XComs, their storage (backed by metadata DB), and limitations (size,
      serialization). Compare classic XComs (xcom_push/xcom_pull) with TaskFlow API automatic passing (return values and @task
      decorator).
  middle:
    Airflow Executors:
    - 'Compare Airflow executors: SequentialExecutor, LocalExecutor, CeleryExecutor, KubernetesExecutor. How does CeleryExecutor
      distribute tasks? What are the pros/cons of KubernetesExecutor vs CeleryExecutor? When to use LocalExecutor in production?'
    Airflow Scheduling:
    - How does Airflow scheduling work? Explain logical date (execution_date), start_date, schedule_interval, catchup, and
      backfill. What is the difference between depends_on_past and wait_for_downstream? How to handle a backfill of historical
      data without triggering downstream DAGs?
    - 'What are Airflow best practices for writing production DAGs? Explain: avoiding top-level code that accesses DB, idempotency,
      idempotent tasks, using idempotent operators, dynamic DAG generation (using Jinja or Python), and idempotent backfills.
      Why should tasks be idempotent?'
    Airflow Operations:
    - 'How to test Airflow DAGs? Explain: DAG validation (airflow dags test), unit testing with pytest, mocking operators/hooks,
      using DagBag for loading tests. How to test a DAG''s task dependencies without running external systems?'
    - How to monitor Airflow health and performance? Explain metrics exposed via StatsD (with Prometheus sink), tracking DAG
      duration, task failures, scheduler lag. How to set up alerts for stuck DAGs, scheduler heartbeat, and celery queue length?
      What is the purpose of Airflow's health endpoint?
    - 'How to secure an Airflow deployment? Explain built-in RBAC (Role-Based Access Control): roles (Admin, User, Op, Viewer),
      permissions on DAGs and resources. How to integrate with LDAP, OAuth (Google, GitHub), or OpenID Connect? What is the
      `fernet_key` used for?'
    Airflow TaskFlow API:
    - What is the TaskFlow API in Airflow 2.0? How does it simplify DAG authoring? Explain @dag and @task decorators, automatic
      XCom passing, and how to use multiple outputs (MultipleOutputs, expand). When should you use TaskFlow vs traditional
      operators?
  senior:
    Airflow Production:
    - What are deferrable operators (or deferrable sensors) in Airflow 2.2+? How do they reduce worker utilization? Explain
      the Trigger concept, Triggerer component, and the difference between classic poke_interval sensor and deferrable mode.
      Show an example of a deferrable operator.
    - 'How to deploy Airflow on Kubernetes? Explain the Helm chart (community or official). Describe the pods: scheduler,
      webserver, workers (Celery), and optional components (Redis, Postgres, Flower). When to use KubernetesExecutor vs CeleryExecutor
      on K8s? How to manage DAGs with GitSync or persistent volume?'
    - 'What are the patterns for dynamic DAG generation in Airflow? Compare static DAGs (many files) vs dynamic generation
      from a config (JSON/YAML). Explain the risks: scheduler performance and DAG parsing time. How to handle large number
      of similar DAGs (e.g., per tenant) without overwhelming the scheduler?'
